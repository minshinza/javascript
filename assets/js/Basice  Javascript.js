// 1: variables
//var
   var name = "ko ko"
    var age = 29;
    var isStudent = true;

    // Let
    let city = "Yangon"
    let area = 45.8;

    // const
    const country = "burma";
    const bq = 9;

    // 2: type 7

    // dispaly the values in the console
   console.log (name)
   console. log (29);
   console.log(isStudent);

   console.log(city);
   console.log(area)

   console.log(country)
   console.log(bq)



  // Type
  var temp =72; //Number
  let greet = "Hello, How are you?"; //string
  const isRain = false; // boolean
  var empty=null; //Null
  let notAssign = ""; // underfined
  let noAssign; // underfined

  let person = {name: "Mg Mg", age:29 }; //object
  let fruits =['apple', 'orange', 'banana']; //array 


  // Check in console using 'typeof' keyword

  console.log(temp);
  console.log(greet);
  console.log(isRain);
  console.log(empty);
  console.log(notAssign);
  console.log(onAssing);
  console.log(person);
  console.log(fruits);
  //==== xxxx ==== //

  // 3. Arithmetic Operatorst ဂဏန်သင်္ချာ

  var sum = 5 + 2; //7
  let diff =10 - 5; // 5
  var product = 3 * 7; // 21
  let divde = 10 / 2; //5 အစား ရလဒ်
  const remainder = 20 % 5; //0 အစားကြွင်း


  // 4. Assignment Operators
  var x = 10;
  x = x + 5; // x += 5;
  x *= 2; // x = x * 2;

  // 5. Comparison Oprerators
  5 == "5"; //true  ဂဏန်း ငါး ၅ ညီသလား ==
  5 === "5"; // false အမျိူး အစား တန်ဖိုး
  5 != "5"; // false  တန်ဖိုးမေး //!မဟုတ်ဘူးလားမေး
  5 !=="5"; // true  မျိုးအစားကို မေးသည်။ အမျိုးအစား မတူဘူးလား

  // 6. Logical Operators
  (ture && false) //false &&က and-နှင့် ။ လက်သဲကွင်းဆို ကွန်းတေးရှင်စစ်
  (true  || false) //true  ||က or-သို့မဟုတ်။ တစ်ခုခု မှန်ရင် ရပြီး
  (!true) //false


   /*
  // 7. String Operators
  var greet = "Hello";
  let name = "ko ko";
  let welcome = greet + "," + name + "!";
   */
  
  // 8. Type Operators
  typeof

 */